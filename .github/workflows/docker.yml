name: Docker CI/CD

on:
  push:
    branches:
      - dev
      - main

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t dtp-image ./app

